<h2>Edit <%= @gym.name %></h2>
<%= link "View Public Page", to: gym_path(@conn, :show, slug(@gym)) %>

<%= render "form.html", changeset: @changeset,
                        action: gym_path(@conn, :update, @gym) %>
