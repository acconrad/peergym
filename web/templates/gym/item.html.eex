<li>
<%= link "Claim your free pass", to: @to_pay, class: "btn btn-lg btn-success pull-right" %>
  <h2><%= link @item.name, to: @to_show %></h2>
  <p class="lead"><%= @item.address %></p>
  <p><%= link @item.phone, to: "tel:#{@item.phone}" %> <strong><%= @item.hours %></strong></p>
</li>
